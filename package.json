{
  "name": "@intelagent/mcp-servers",
  "version": "1.0.0",
  "private": true,
  "description": "Open-source MCP servers by Intelagent",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w packages/enrichment && npm run build -w packages/create-intelagent-mcp",
    "test": "npm run test --workspaces --if-present",
    "clean": "rm -rf packages/*/dist",
    "typecheck": "npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/IntelagentStudios/Intelagent-MCPs.git"
  },
  "author": "Intelagent Studios",
  "license": "MIT"
}
